apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-deploy-test
  labels:
    app: my-back-test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-back-test
  template:
    metadata:
      labels:
        app: my-back-test
    spec:
      containers:
        - name: front-container
          image: dockerhub.timeweb.cloud/hapsy/webportal-back:pr-11
          ports:
            - containerPort: 5000
          env:
            - name: DATABASE_URL
              value: postgresql://postgresadmin:admin123@postgres-test.postgresql.svc.cluster.local:5432/postgresdb-test?schema=public
      initContainers:
        - name: init-front-container
          image: dockerhub.timeweb.cloud/hapsy/webportal-back:pr-11
          command:
            - npx
            - prisma
            - migrate
            - deploy
          env:
            - name: DATABASE_URL
              value: postgresql://postgresadmin:admin123@postgres-test.postgresql.svc.cluster.local:5432/postgresdb-test?schema=public
